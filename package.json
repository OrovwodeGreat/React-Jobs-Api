{
  "name": "react-jobs-api",
  "version": "1.0.0",
  "scripts": {
    "build": "react-scripts build",
    "start": "react-scripts start",
    "server": "json-server --watch src/jobs.json --port 8000"
  },
  "dependencies": {
    "json-server": "^0.17.3",
    "react-scripts": "^5.0.1"
  }
}
