{
  "name": "react-jobs-api",
  "version": "1.0.0",
  "scripts": {
    "build": "react-scripts build",
    "start": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.3",
    "react-scripts": "^5.0.1"
  }
}
